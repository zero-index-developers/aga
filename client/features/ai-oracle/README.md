# AI Oracle Feature

Frontend-only AI Oracle API helpers, types, and hooks belong here. The current Oracle panel stays in `client/components/architecture` because it is part of the graph workspace surface.
