# Auth Feature

Frontend-only auth feature code belongs here when it is not a shared UI component or route file. The current auth forms remain in `client/components/auth` because they are directly reused by App Router pages.
